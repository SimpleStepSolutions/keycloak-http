apiVersion: v1
kind: Secret
metadata:
  name: realm-secret-external
  labels:
    app: {{ .Chart.Name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  external-realm.json: {{ tpl (.Files.Get "files/external-realm.json") . | b64enc }}